% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autotunegg.R
\name{autotunegg}
\alias{autotunegg}
\title{Fast and automatic tuning and fitting for group lasso penalized least squares}
\usage{
autotunegg(
  x,
  y,
  group,
  alpha = 0.01,
  standardize = T,
  standarize_response = T,
  intercept = T,
  active = NULL,
  trace_it = FALSE,
  sigma_tolerance = 1e-08,
  beta_tolerance = 1e-08,
  sigma_iter_max = 100,
  beta_iter_max = 100,
  active_iter_max = 100,
  tau = 0.5
)
}
\arguments{
\item{x}{Matrix of predictors, of dimension \eqn{n \times p} with \eqn{n}.
observations of \eqn{p} variables}

\item{y}{Vector of responses, length \eqn{n}.}

\item{group}{Vector of consecutive integer group labels, length \eqn{p}}

\item{alpha}{Significance level for sequential Wald tests, default is 0.01}

\item{standardize}{Whether to standardize prior to tuning and fitting, default
is \code{TRUE}.
Coefficients are transformed back to original scale after model convergence.
Strongly recommended as this algorithm does not implement standardized group
lasso.}

\item{standarize_response}{Whether Y is demeaned, default is \code{TRUE}.}

\item{intercept}{Whether to include an intercept, default is \code{TRUE}.}

\item{active}{Whether to implement active set selection. Default is \code{TRUE} when
all groups have 5 or more membes, and \code{FALSE} otherwise.}

\item{trace_it}{Whether to print out iteration details, default is \code{FALSE}.}

\item{sigma_tolerance}{Convergence termination tolerance for the initial
tuning of \eqn{\sigma^2} and \eqn{\lambda}. Default is \eqn{1e-8}.}

\item{beta_tolerance}{Convergence termination tolerance after
\eqn{\sigma^2} and \eqn{\lambda} are obtained. Default is \eqn{1e-8}.}

\item{sigma_iter_max}{Maximum number of iterations allowed for the initial
tuning of \eqn{\sigma^2} and \eqn{\lambda}. Default is \code{100}.}

\item{beta_iter_max}{Maximum  number of iterations allowed after \eqn{\sigma^2}
and \eqn{\lambda} are obtained. Default is \code{100}.}

\item{active_iter_max}{If \code{active = TRUE}, limits the number of updates
to the active support set. Default is \code{100}.}

\item{tau}{Scaling for initialization of \eqn{\lambda}. Initial
\eqn{\lambda = \lambda_{\max} \times \tau}. Must be smaller than \code{1} or the
algorithm will get stuck. This scaling is removed once \eqn{\hat{\sigma}^2}
is smaller than \eqn{\text{Var}(Y)}.}
}
\value{
A list with final model fit as well as regularization path details.
}
\description{
Fast and automatic tuning and fitting for group lasso penalized least squares
}
